/**
 * Image Gallery Block
 * Displays images in a two-column grid with pink background
 *
 * Content structure:
 * - Row containing columns
 * - Each column contains a picture element with an image
 */
export default function decorate(block) {
  // The CSS handles all styling via direct child selectors
  // No additional JavaScript decoration needed for this block
}
